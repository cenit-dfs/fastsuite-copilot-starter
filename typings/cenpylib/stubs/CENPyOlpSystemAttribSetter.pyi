"""
Automatically generated module. COPYRIGHT CENIT AG 2026.
"""

from centypes import *

class CENPyOlpSystemAttribSetter:
   def SetTranslationX(self, transX: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         transX: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetTranslationY(self, transY: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         transY: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetTranslationZ(self, transZ: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         transZ: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetRotationRx(self, rotX: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         rotX: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetRotationRy(self, rotY: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         rotY: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetRotationRz(self, rotZ: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         rotZ: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetCutterCompensation(self, cutComp: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         cutComp: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetOffsetX(self, offset: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         offset: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetOffsetY(self, offset: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         offset: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetOffsetZ(self, offset: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         offset: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetSheetOffset(self, offset: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         offset: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetOffsetRx(self, offset: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         offset: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetOffsetRy(self, offset: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         offset: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetOffsetRz(self, offset: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         offset: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetAlignmentRx(self, alignX: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         alignX: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetAlignmentRy(self, alignY: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         alignY: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetAlignmentRz(self, alignZ: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         alignZ: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetApproximType(self, approxType: int, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         approxType: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetMaxSag(self, maxSag: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         maxSag: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetMaxDist(self, maxDist: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         maxDist: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetMinRadius(self, minRad: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         minRad: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetMaxRadius(self, maxRad: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         maxRad: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetStartPointValue(self, value: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         value: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetEndPointValue(self, value: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         value: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetApproachDirection(self, dir: bool, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         dir: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetProcessFlowDirection(self, dir: bool, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set a new value for the standard system attribute if it exists.
      
      Args:
         dir: New value to set.
         overrideMode: Override mode.
      """
      ...
   
   def SetCuttingPlaneXpos(self, posX: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set the olp attribute for the X position of the cutting plane from the attribute container of the underlying attribute object.
      
      Args:
         posX: X position of the cutting plane, relative to the workpiece's root frame.
         overrideMode: If AttribOverrideMode::OperationLevel, find the attribute in the group or program and override it in the operation.
      """
      ...
   
   def SetCuttingPlaneYpos(self, posY: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set the olp attribute for the Y position of the cutting plane from the attribute container of the underlying attribute object.
      
      Args:
         posY: Y position of the cutting plane, relative to the workpiece's root frame.
         overrideMode: If AttribOverrideMode::OperationLevel, find the attribute in the group or program and override it in the operation.
      """
      ...
   
   def SetCuttingPlaneZpos(self, posZ: float, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set the olp attribute for the Z position of the cutting plane from the attribute container of the underlying attribute object.
      
      Args:
         posZ: Z position of the cutting plane, relative to the workpiece's root frame.
         overrideMode: If AttribOverrideMode::OperationLevel, find the attribute in the group or program and override it in the operation.
      """
      ...
   
   def SetCuttingPlaneXrot(self, rotX: float, propertyChanged: bool, avoidRecompute: bool, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set the olp attribute for the rotation around X axis of the cutting plane (manipulator's Rx field)
      from the attribute container of the underlying attribute object.
      
      Args:
         rotX: X rotation of the cutting plane, relative to the workpiece's root frame.
         propertyChanged: True if property changed event should be called, otherwise False.
         avoidRecompute: Temporarily set ComputeEnterState to None to avoid a recompute when setting the attribute.
         overrideMode: If AttribOverrideMode::OperationLevel, find the attribute in the group or program and override it in the operation.
      """
      ...
   
   def SetCuttingPlaneYrot(self, rotY: float, propertyChanged: bool, avoidRecompute: bool, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set the olp attribute for the rotation around Y axis of the cutting plane (manipulator's Ry field)
      from the attribute container of the underlying attribute object.
      
      Args:
         rotY: Y rotation of the cutting plane, relative to the workpiece's root frame.
         propertyChanged: True if property changed event should be called, otherwise False.
         avoidRecompute: Temporarily set ComputeEnterState to None to avoid a recompute when setting the attribute.
         overrideMode: If AttribOverrideMode::OperationLevel, find the attribute in the group or program and override it in the operation.
      """
      ...
   
   def SetCuttingPlaneZrot(self, rotZ: float, propertyChanged: bool, avoidRecompute: bool, overrideMode: int=ATTRIBOVERRIDEMODE_DEFAULT):
      """Set the olp attribute for the rotation around Z axis of the cutting plane (manipulator's Rz field)
      from the attribute container of the underlying attribute object.
      
      Args:
         rotZ: Z rotation of the cutting plane, relative to the workpiece's root frame.
         propertyChanged: True if property changed event should be called, otherwise False.
         avoidRecompute: Temporarily set ComputeEnterState to None to avoid a recompute when setting the attribute.
         overrideMode: If AttribOverrideMode::OperationLevel, find the attribute in the group or program and override it in the operation.
      """
      ...
   
   def SetGuidedCurveAxis(self, axisLiteralValue: str, propertyChanged: bool, avoidRecompute: bool):
      """Set the olp attribute for aligning the cutting plane's normal (blue Z axis) to a world axis.
      
      Args:
         axisLiteralValue: Values representing the names of the world axes.
         propertyChanged: True if property changed event should be called, otherwise False.
         avoidRecompute: Temporarily set ComputeEnterState to None to avoid a recompute when setting the attribute.
      """
      ...
   
