"""
Automatically generated module. COPYRIGHT CENIT AG 2026.
"""

from .CENPyOlpAttributeBool import *
from .CENPyOlpAttributeDouble import *
from .CENPyOlpAttributeEnum import *

class CENPyOlpSystemAttribGetter:
   def GetTranslationX(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetTranslationY(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetTranslationZ(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetRotationRx(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetRotationRy(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetRotationRz(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetCutterCompensation(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetOffsetX(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetOffsetY(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetOffsetZ(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetSheetOffset(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetOffsetRx(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetOffsetRy(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetOffsetRz(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetAlignmentRx(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetAlignmentRy(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetAlignmentRz(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetApproximType(self) -> CENPyOlpAttributeEnum:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetMaxSag(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetMaxDist(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetMinRadius(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetMaxRadius(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetStartPointValue(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetEndPointValue(self) -> CENPyOlpAttributeDouble:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetApproachDirection(self) -> CENPyOlpAttributeBool:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetProcessFlowDirection(self) -> CENPyOlpAttributeBool:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetSwapUpperLowerGuideCurve(self) -> CENPyOlpAttributeBool:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetAutoCreateTangentAbsolute(self) -> CENPyOlpAttributeBool:
      """Get a standard system attribute.
      
      Returns:
         Attribute object.
      """
      ...
   
   def GetCuttingPlaneXpos(self) -> CENPyOlpAttributeDouble:
      """Get the olp attribute for the X position of the cutting plane 
      from the attribute container of the underlying attribute object.
      
      Returns:
         Attribute with a value which is relative to the workpiece's root frame.
      """
      ...
   
   def GetCuttingPlaneYpos(self) -> CENPyOlpAttributeDouble:
      """Gets the olp attribute for the Y position of the cutting plane
      from the attribute container of the underlying attribute object.
      
      Returns:
         Attribute with a value which is relative to the workpiece's root frame.
      """
      ...
   
   def GetCuttingPlaneZpos(self) -> CENPyOlpAttributeDouble:
      """Get the olp attribute for the Z position of the cutting plane
      from the attribute container of the underlying attribute object.
      
      Returns:
         Attribute with a value which is relative to the workpiece's root frame.
      """
      ...
   
   def GetCuttingPlaneXrot(self) -> CENPyOlpAttributeDouble:
      """Get the olp attribute for the rotation around X axis of the cutting plane (manipulator's Rx field)
      from the attribute container of the underlying attribute object.
      
      Returns:
         Attribute with a value which is relative to the workpiece's root frame.
      """
      ...
   
   def GetCuttingPlaneYrot(self) -> CENPyOlpAttributeDouble:
      """Get the olp attribute for the rotation around Y axis of the cutting plane (manipulator's Ry field)
      from the attribute container of the underlying attribute object.
      
      Returns:
         Attribute with a value which is relative to the workpiece's root frame.
      """
      ...
   
   def GetCuttingPlaneZrot(self) -> CENPyOlpAttributeDouble:
      """Get the olp attribute for the rotation around Z axis of the cutting plane (manipulator's Rz field)
      from the attribute container of the underlying attribute object.
      
      Returns:
         Attribute with a value which is relative to the workpiece's root frame.
      """
      ...
   
   def GetGuidedCurveAxis(self) -> CENPyOlpAttributeEnum:
      """Get the olp attribute for aligning the cutting plane's normal (blue Z axis) to a world axis
      from the attribute container of the underlying attribute object.
      
      Returns:
         Attribute with a value which represents a world axis.
      """
      ...
   
