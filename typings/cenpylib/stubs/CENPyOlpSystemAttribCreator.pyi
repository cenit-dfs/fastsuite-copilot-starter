"""
Automatically generated module. COPYRIGHT CENIT AG 2026.
"""

from .CENPyOlpAttributeBool import *
from .CENPyOlpAttributeDouble import *
from .CENPyOlpAttributeEnum import *

class CENPyOlpSystemAttribCreator:
   def AddTranslationX(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddTranslationY(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddTranslationZ(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddRotationRx(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddRotationRy(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddRotationRz(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddCutterCompensation(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddOffsetX(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddOffsetY(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddOffsetZ(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddSheetOffset(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddOffsetRx(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddOffsetRy(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddOffsetRz(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddAlignmentRx(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddAlignmentRy(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddAlignmentRz(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddApproximType(self, approxType: int, olpAttribType: int) -> CENPyOlpAttributeEnum:
      """Create a standard system attribute with a defined approximation and attribute types.
      
      Args:
         approxType: Contour approximation type.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddMaxSag(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddMaxDist(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddMinRadius(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddMaxRadius(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddStartPoint(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddEndPoint(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         val: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddApproachDirection(self, positive: bool, olpAttribType: int) -> CENPyOlpAttributeBool:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         positive: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddProcessFlowDirection(self, positive: bool, olpAttribType: int) -> CENPyOlpAttributeBool:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         positive: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddSwapUpperLowerGuideCurve(self, swap: bool, olpAttribType: int) -> CENPyOlpAttributeBool:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         swap: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddAutoCreateTangentAbsolute(self, autoCreate: bool, olpAttribType: int) -> CENPyOlpAttributeBool:
      """Create a standard system attribute with a defined value and attribute type. Overrides the attribute parameters if it already exists.
      
      Args:
         autoCreate: Value to set.
         olpAttribType: Attribute type.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddCuttingPlaneXpos(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create an attribute for the X position of the cutting plane
      and stores it inside the attribute container of the underlying attribute object.
      
      Args:
         val: X position of the cutting plane, relative to the workpiece's root frame.
         olpAttribType: Mask used for determining the domain of visibility of the attribute, 
      ///   for possible values, look for "OPERATION_ATTRIBUTE" in CommonIfaceDefs.h.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddCuttingPlaneYpos(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Create an attribute for the Y position of the cutting plane
      and stores it inside the attribute container of the underlying attribute object.
      
      Args:
         val: Y position of the cutting plane, relative to the workpiece's root frame.
         olpAttribType: Mask used for determining the domain of visibility of the attribute, 
      ///   for possible values, look for "OPERATION_ATTRIBUTE" in CommonIfaceDefs.h
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddCuttingPlaneZpos(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Creates an attribute for the Z position of the cutting plane
      and stores it inside the attribute container of the underlying attribute object.
      
      Args:
         val: Z position of the cutting plane, relative to the workpiece's root frame.
         olpAttribType: Mask used for determining the domain of visibility of the attribute, 
      ///   for possible values, look for "OPERATION_ATTRIBUTE" in CommonIfaceDefs.h.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddCuttingPlaneXrot(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Creates an attribute for the rotation around X axis of the cutting plane (manipulator's Rx field)
      and stores it inside the attribute container of the underlying attribute object.
      
      Args:
         val: X rotation of the cutting plane, relative to the workpiece's root frame.
         olpAttribType: Mask used for determining the domain of visibility of the attribute, 
      ///   for possible values, look for "OPERATION_ATTRIBUTE" in CommonIfaceDefs.h.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddCuttingPlaneYrot(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Creates an attribute for the rotation around Y axis of the cutting plane (manipulator's Ry field)
      and stores it inside the attribute container of the underlying attribute object.
      
      Args:
         val: Y rotation of the cutting plane, relative to the workpiece's root frame.
         olpAttribType: Mask used for determining the domain of visibility of the attribute, 
      ///   for possible values, look for "OPERATION_ATTRIBUTE" in CommonIfaceDefs.h.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddCuttingPlaneZrot(self, val: float, olpAttribType: int) -> CENPyOlpAttributeDouble:
      """Creates an attribute for the rotation around Z axis of the cutting plane (manipulator's Rz field)
      and stores it inside the attribute container of the underlying attribute object.
      
      Args:
         val: Z rotation of the cutting plane, relative to the workpiece's root frame.
         olpAttribType: Mask used for determining the domain of visibility of the attribute, 
      ///   for possible values, look for "OPERATION_ATTRIBUTE" in CommonIfaceDefs.h.
      
      Returns:
         Created attribute object.
      """
      ...
   
   def AddGuidedCurveAxis(self, literalValues: list, olpAttribType: int) -> CENPyOlpAttributeEnum:
      """Creates an attribute for aligning the cutting plane's normal (blue Z axis) to a world axis.
      
      Args:
         literalValues: literal values representing the names of the axes.
         olpAttribType: Mask used for determining the domain of visibility of the attribute, 
      ///   for possible values, look for "OPERATION_ATTRIBUTE" in CommonIfaceDefs.h.
      
      Returns:
         Created attribute object.
      """
      ...
   
